// @ts-check

/**
 * Groups the app-level bindings consumed by the chart-library import helpers.
 *
 * @param {object} [options]
 * @returns {Record<string, any>}
 */
export function createChartLibraryImportBindings(options = {}) {
  return { ...options };
}
