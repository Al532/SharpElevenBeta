// @ts-check

/**
 * Groups the app-level bindings consumed by the chart import-status updater.
 *
 * @param {object} [options]
 * @returns {Record<string, any>}
 */
export function createChartImportStatusBindings(options = {}) {
  return { ...options };
}
