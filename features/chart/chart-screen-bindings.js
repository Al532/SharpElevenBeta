// @ts-check

/**
 * Groups the app-level bindings consumed by the chart screen initializer.
 *
 * @param {object} [options]
 * @returns {Record<string, any>}
 */
export function createChartScreenBindings(options = {}) {
  return { ...options };
}
