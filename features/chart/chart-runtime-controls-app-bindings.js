// @ts-check

/**
 * Groups the app-level bindings consumed by the chart runtime-controls binder.
 *
 * @param {object} [options]
 * @returns {Record<string, any>}
 */
export function createChartRuntimeControlsAppBindings(options = {}) {
  return { ...options };
}
