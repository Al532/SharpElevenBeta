// @ts-check

/**
 * Groups the app-level bindings consumed by the chart bar-selection handler.
 *
 * @param {object} [options]
 * @returns {Record<string, any>}
 */
export function createChartBarSelectionBindings(options = {}) {
  return { ...options };
}
