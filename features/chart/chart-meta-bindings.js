// @ts-check

/**
 * Groups the app-level bindings consumed by the chart meta renderer.
 *
 * @param {object} [options]
 * @returns {Record<string, any>}
 */
export function createChartMetaBindings(options = {}) {
  return { ...options };
}
