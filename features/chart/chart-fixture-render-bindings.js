// @ts-check

/**
 * Groups the app-level bindings consumed by the selected-chart renderer.
 *
 * @param {object} [options]
 * @returns {Record<string, any>}
 */
export function createChartFixtureRenderBindings(options = {}) {
  return { ...options };
}
