// @ts-check

/**
 * Groups the app-level bindings consumed by the chart selector renderer.
 *
 * @param {object} [options]
 * @returns {Record<string, any>}
 */
export function createChartSelectorBindings(options = {}) {
  return { ...options };
}
