// @ts-check

/**
 * Groups the app-level bindings consumed by the chart runtime controls
 * installer.
 *
 * @param {object} [options]
 * @returns {Record<string, any>}
 */
export function createChartRuntimeControlsBindings(options = {}) {
  return { ...options };
}
