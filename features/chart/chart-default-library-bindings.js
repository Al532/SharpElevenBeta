// @ts-check

/**
 * Groups the app-level bindings consumed by the default chart-library importer.
 *
 * @param {object} [options]
 * @returns {Record<string, any>}
 */
export function createChartDefaultLibraryBindings(options = {}) {
  return { ...options };
}
