// @ts-check

/**
 * Groups the app-level bindings consumed by the chart navigation controller.
 *
 * @param {object} [options]
 * @returns {Record<string, any>}
 */
export function createChartNavigationBindings(options = {}) {
  return { ...options };
}
