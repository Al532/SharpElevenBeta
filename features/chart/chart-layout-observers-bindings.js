// @ts-check

/**
 * Groups the app-level bindings consumed by the chart layout-observers binder.
 *
 * @param {object} [options]
 * @returns {Record<string, any>}
 */
export function createChartLayoutObserversBindings(options = {}) {
  return { ...options };
}
