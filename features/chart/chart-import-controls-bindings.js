// @ts-check

/**
 * Groups the app-level bindings consumed by the chart import controls.
 *
 * @param {object} [options]
 * @returns {Record<string, any>}
 */
export function createChartImportControlsBindings(options = {}) {
  return { ...options };
}
