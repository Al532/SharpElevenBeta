// @ts-check

/**
 * Groups the app-level bindings consumed by the chart navigation-state updater.
 *
 * @param {object} [options]
 * @returns {Record<string, any>}
 */
export function createChartNavigationStateBindings(options = {}) {
  return { ...options };
}
