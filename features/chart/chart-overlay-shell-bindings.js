// @ts-check

/**
 * Groups the app-level bindings consumed by the chart overlay shell helpers.
 *
 * @param {object} [options]
 * @returns {Record<string, any>}
 */
export function createChartOverlayShellBindings(options = {}) {
  return { ...options };
}
