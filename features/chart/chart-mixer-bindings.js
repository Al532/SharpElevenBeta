// @ts-check

/**
 * Groups the app-level bindings consumed by the chart mixer-output updater.
 *
 * @param {object} [options]
 * @returns {Record<string, any>}
 */
export function createChartMixerBindings(options = {}) {
  return { ...options };
}
