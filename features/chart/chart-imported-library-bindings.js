// @ts-check

/**
 * Groups the app-level bindings consumed by the imported-chart library applier.
 *
 * @param {object} [options]
 * @returns {Record<string, any>}
 */
export function createChartImportedLibraryBindings(options = {}) {
  return { ...options };
}
