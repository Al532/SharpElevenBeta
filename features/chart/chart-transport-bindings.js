// @ts-check

/**
 * Groups the app-level bindings consumed by the chart transport renderer.
 *
 * @param {object} [options]
 * @returns {Record<string, any>}
 */
export function createChartTransportBindings(options = {}) {
  return { ...options };
}
