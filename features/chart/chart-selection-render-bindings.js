// @ts-check

/**
 * Groups the app-level bindings consumed by the chart selection-state renderer.
 *
 * @param {object} [options]
 * @returns {Record<string, any>}
 */
export function createChartSelectionRenderBindings(options = {}) {
  return { ...options };
}
