// @ts-check

/**
 * Groups the app-level bindings consumed by the chart popover helpers.
 *
 * @param {object} [options]
 * @returns {Record<string, any>}
 */
export function createChartPopoverBindings(options = {}) {
  return { ...options };
}
