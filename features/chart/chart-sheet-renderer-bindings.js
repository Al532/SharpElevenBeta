// @ts-check

/**
 * Groups the app-level bindings consumed by the chart sheet renderer.
 *
 * @param {object} [options]
 * @returns {Record<string, any>}
 */
export function createChartSheetRendererBindings(options = {}) {
  return { ...options };
}
