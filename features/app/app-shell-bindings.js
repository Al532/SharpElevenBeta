// @ts-check

/**
 * Groups the app-level bindings consumed by the shared app-shell initializer.
 *
 * @param {object} [options]
 * @returns {Record<string, any>}
 */
export function createAppShellBindings(options = {}) {
  return { ...options };
}
